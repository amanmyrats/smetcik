import { BaseCompanyResource } from './base-company-resource.model';

describe('BaseCompanyResource', () => {
  it('should create an instance', () => {
    expect(new BaseCompanyResource()).toBeTruthy();
  });
});
