import { BaseCompanyCountry } from './base-company-country.model';

describe('BaseCompanyCountry', () => {
  it('should create an instance', () => {
    expect(new BaseCompanyCountry()).toBeTruthy();
  });
});
