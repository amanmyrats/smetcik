import { BaseCompanyTrade } from './base-company-trade.model';

describe('BaseCompanyTrade', () => {
  it('should create an instance', () => {
    expect(new BaseCompanyTrade()).toBeTruthy();
  });
});
