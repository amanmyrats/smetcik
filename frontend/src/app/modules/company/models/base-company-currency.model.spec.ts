import { BaseCompanyCurrency } from './base-company-currency.model';

describe('BaseCompanyCurrency', () => {
  it('should create an instance', () => {
    expect(new BaseCompanyCurrency()).toBeTruthy();
  });
});
