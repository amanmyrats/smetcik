import { MaterialExtraInfo } from './material-extra-info.class';

describe('MaterialExtraInfo', () => {
  it('should create an instance', () => {
    expect(new MaterialExtraInfo()).toBeTruthy();
  });
});
