import { PaginatedBoqItem } from './paginated-boq-item.models';

describe('PaginatedBoqItem', () => {
  it('should create an instance', () => {
    expect(new PaginatedBoqItem()).toBeTruthy();
  });
});
